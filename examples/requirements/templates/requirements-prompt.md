# Requirements Gathering — Interview Workflow Prompt

# Requirements Gathering — Interview Workflow Prompt (example reference)

This file is a short pointer to the canonical template in `templates/requirements-prompt.md`.

Canonical template: `templates/requirements-prompt.md`

Example usage:
1. Copy `templates/requirements-prompt.md` to your editing area and run the interview per instructions.
2. Save the output under `docs/implementation-plan/m{n}/` as `requirements-interview.md` and `requirements.md`.

Keep filled examples under `examples/requirements/inputs/` (this folder contains a filled example input for the project).
